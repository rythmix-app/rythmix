PORT=3333
HOST=0.0.0.0
LOG_LEVEL=info
NODE_ENV=production
# APP_KEY is used for cryptographic operations (e.g., session signing, encryption).
# Generate a secure random key for production, e.g.:
#   openssl rand -base64 32
APP_KEY=your-production-app-key-here
DB_HOST=0.0.0.0
DB_PORT=5432
DB_USER=root
DB_PASSWORD=root
DB_DATABASE=rythmix